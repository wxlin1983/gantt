# Gantt 流程管理系統 — 設計文件（UI / UX）

本文件描述使用者實際感知得到的部分：畫面、操作流程、互動回饋與設計語彙。
系統架構與實作規格請見 [implement.md](implement.md)。

---

## 1. 產品定位

這套系統解決的問題是：**同一種流程要跑很多次，每次的參數與日期不同，但步驟結構相同。**

使用者不該每次都手動畫一張 Gantt。他應該：

1. 挑一個已經定義好的流程模板
2. 填入這次的參數與**目標完成日期**
3. 系統自動反推出「為了在那天完成，每一步該在何時開始、何時結束」
4. 流程跑起來之後，有些步驟由人手動回報完成，有些由後端 API 自動回報
5. 隨時看得到：現在到哪一步、會不會來不及、卡在誰身上

因此本系統的核心體驗是**「反推時程」＋「進度追蹤」**，而不是傳統專案管理軟體的「手動排時間軸」。

### 1.1 使用者角色

| 角色 | 他關心什麼 | 主要使用的畫面 |
|---|---|---|
| **流程負責人** (Case Owner) | 這個 case 會不會準時？現在卡在哪？ | Case 總表、Case 詳情 (Gantt) |
| **任務執行者** (Task Owner) | 哪些事情輪到我做？什麼時候要交？ | 我的任務、Case 詳情 |
| **模板管理員** (Template Admin) | 流程定義對不對？參數夠不夠用？ | Template 編輯器、Task Template 庫 |

一個人可以同時是多種身分。差別只反映在「能不能編輯 template」與「能不能標記某個 task 完成」，不影響瀏覽。

> **用詞區分**：上表講的是**系統身分**（決定權限）。另有一個不同概念叫**模板角色**（template role）——模板宣告「這個流程需要一位專案經理、一位品保負責人」，建立 case 時才綁定實際的人（見 [流程 A 步驟 2](#3-流程-a從-template-建立-case)）。兩者不相干：模板角色是流程的人力槽位，系統身分是存取控制。UI 上一律稱前者為「角色」、後者為「權限」，避免混淆。

---

## 2. 資訊架構與導覽

```
┌─ 頂部導覽列 ────────────────────────────────────────────────────┐
│  [Logo]  Case 總表   我的任務(3)   Template 庫   Task 庫   🔔  👤 │
└─────────────────────────────────────────────────────────────────┘
```

| 導覽項 | 路徑 | 說明 |
|---|---|---|
| **Case 總表** | `/cases` | 預設首頁。所有 gantt case 的清單與健康狀態 |
| **Case 詳情** | `/cases/:id` | 單一 case 的 Gantt 圖與任務細節 |
| **我的任務** | `/my-tasks` | 跨 case 的個人待辦，徽章顯示待處理數量 |
| **Template 庫** | `/templates` | Gantt 模板清單與編輯器 |
| **Task 庫** | `/task-templates` | Task 模板清單與編輯器 |
| **通知** | 下拉面板 | 指派、即將到期、逾期、API 失敗 |

導覽層級刻意壓平在兩層以內。Case 詳情是使用者停留最久的畫面，從任何地方都能一鍵抵達。

---

## 3. 流程 A：從 Template 建立 Case

四步精靈，可隨時返回上一步，離開前有未儲存提示。

### 步驟 1 — 選擇模板

```
┌──────────────────────────────────────────────────────────────┐
│  建立新 Case                                     步驟 1 / 4  │
│  ─────●────────○────────○────────○                          │
│                                                              │
│  🔍 搜尋模板...                            [全部 ▾] [我的 ▾] │
│                                                              │
│  ┌────────────────────────┐  ┌────────────────────────┐    │
│  │ 新產品導入流程          │  │ 月結關帳流程            │    │
│  │ v3 · 12 個步驟          │  │ v7 · 8 個步驟           │    │
│  │ 典型工期 ~14 天         │  │ 典型工期 ~3 天          │    │
│  │ 使用中 8 個 case        │  │ 使用中 2 個 case        │    │
│  │              [ 選擇 ]   │  │              [ 選擇 ]   │    │
│  └────────────────────────┘  └────────────────────────┘    │
└──────────────────────────────────────────────────────────────┘
```

每張卡片顯示：模板名稱、目前版本、步驟數、**典型工期**（依預設參數試算出的關鍵路徑長度，讓使用者在選之前就知道大概要多久）、目前使用中的 case 數量。

點卡片任一處可展開唯讀的流程縮圖預覽，不必進編輯器就能確認選對了。

### 步驟 2 — 填寫參數

參數表單依模板的 `template_para` **動態生成**，型別決定欄位控制項：

| `para_type` | UI 控制項 |
|---|---|
| `int` / `float` | 數字輸入框，含 min/max 驗證與步進按鈕 |
| `str` | 單行文字框（若定義了 `max_length` 則顯示字數） |
| `bool` | 開關 (toggle) |
| `date` | 日期選擇器 |
| `enum` | 下拉選單，選項來自 `choices` |

```
┌──────────────────────────────────────────────────────────────┐
│  建立新 Case                                     步驟 2 / 4  │
│  ─────✓────────●────────○────────○                          │
│                                                              │
│  Case 名稱 *                                                 │
│  ┌────────────────────────────────────────────────────┐     │
│  │ 2026Q3 新產品導入 - 型號 A                          │     │
│  └────────────────────────────────────────────────────┘     │
│                                                              │
│  模板參數                                                    │
│                                                              │
│  測試工時 (my_para1) *                    ⓘ 影響測試階段工期 │
│  ┌──────────┐                                               │
│  │   12   ⬍ │  預設 12                                      │
│  └──────────┘                                               │
│                                                              │
│  產線代號 (my_para2) *                                       │
│  ┌────────────────────────┐                                 │
│  │ test                   │  預設 test                      │
│  └────────────────────────┘                                 │
│                                                              │
│  ────────────────────────────────────────────────────────   │
│  角色指派                                                    │
│                                                              │
│  專案經理 (pm) *                                             │
│  ┌────────────────────────┐                                 │
│  │ 👤 王小明            ▾ │                                 │
│  └────────────────────────┘                                 │
│    將負責：需求確認、結案報告                                 │
│                                                              │
│  品保負責人 (qa_lead) *                        建議來自 品保部│
│  ┌────────────────────────┐                                 │
│  │ 👤 陳大文            ▾ │                                 │
│  └────────────────────────┘                                 │
│    將負責：功能測試、耐久測試                                 │
│                                                              │
│                              [ 上一步 ]  [ 下一步 ]          │
└──────────────────────────────────────────────────────────────┘
```

設計要點：

- 所有欄位**預先填入 `para_default`**，使用者只需修改需要改的。一路按下一步也能建出一個合理的 case。
- 每個欄位旁的 ⓘ 說明「這個參數會影響什麼」——因為參數可被 task 的工期引用（例如 `{{ para.my_para1 }}H`），也可能決定要展開幾個平行任務或跳過哪些步驟，改動它會直接改變流程形狀。編輯器在定義參數時可填寫這段說明。
- 參數多時依模板定義的 `group` 分區塊顯示，避免一長條表單。
- **角色指派**是獨立區塊。模板不寫死人名，而是宣告「這個流程需要一位專案經理、一位品保負責人」，建立 case 時才綁定實際的人。每個角色下方即時列出「他將負責哪些步驟」，讓指派者知道自己交付了什麼工作量。
- 角色選擇器依模板的 `default_group` 預先篩選候選人，但不強制——臨時借調他部門的人是常態。
- 必填未填時，錯誤訊息出現在欄位下方而非頂端統一列出。

### 步驟 3 — 設定目標日期

```
┌──────────────────────────────────────────────────────────────┐
│  建立新 Case                                     步驟 3 / 4  │
│  ─────✓────────✓────────●────────○                          │
│                                                              │
│  目標完成日期 *                                              │
│  ┌────────────────────────┐  ┌──────────┐                   │
│  │ 2026-08-15             │  │  18:00   │                   │
│  └────────────────────────┘  └──────────┘                   │
│                                                              │
│  ⓘ 系統會從這個時間點往回推算每個步驟的起訖時間。             │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ ⚠️ 依目前參數，此流程需要 36 小時                     │   │
│  │    另含專案緩衝 8 小時（模板設定）                     │   │
│  │    最早需在 2026-08-13 22:00 開始                     │   │
│  │    距現在還有 16 天餘裕                                │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│                              [ 上一步 ]  [ 下一步 ]          │
└──────────────────────────────────────────────────────────────┘
```

輸入目標日期後**即時試算**並顯示三種可能結論：

| 狀況 | 呈現 |
|---|---|
| 有餘裕 | 🟢 綠色資訊框，顯示「距現在還有 N 天餘裕」 |
| 剛好 / 緊迫（餘裕 < 20% 工期） | 🟡 黃色警示框，顯示「需立即開始」 |
| 來不及（反推起始時間早於現在） | 🔴 紅色警示框，顯示「回推起始時間已過去 N 小時」，並提供 **「改用最早可行日期」** 一鍵按鈕 |

**關於專案緩衝。** 反推排程有個容易被忽略的性質：每一步的結束時間剛好接著下一步的開始時間，中間沒有任何空隙。換句話說，反推出來的是一份「每一步都不能出錯」的計畫，任一步延遲一小時，交期就晚一小時。

模板可以宣告一段**專案緩衝**，反推時從「目標日期減去緩衝」起算。緩衝集中放在末端而不是每個步驟各加保險——分散的保險會被工作自然填滿而看不出來被誰用掉，集中的緩衝才是一塊可量測、可歸因的共享資源。它在 Gantt 上是一段獨立的斜紋區塊，不屬於任何人（見 §4.1）。

**來不及時仍允許繼續建立**——現實中流程本來就常常一開始就落後，強制擋住只會讓使用者去改假數字。系統會照實建立並在 Gantt 上標為逾期。

### 步驟 4 — 時程預覽與確認

建立**之前**就顯示完整 Gantt 預覽圖，這是本流程最重要的設計決定：使用者不必先建再看再刪。

```
┌──────────────────────────────────────────────────────────────┐
│  建立新 Case                                     步驟 4 / 4  │
│  ─────✓────────✓────────✓────────●                          │
│                                                              │
│  08/13        08/14              08/15                       │
│  ├──────────────┼──────────────────┼──────────┤ 🎯          │
│                                                              │
│  ▼ 準備階段                                                  │
│    需求確認  (王小明 / 研發)    ▓▓▓▓▓▓                      │
│                                       ⋯↘  ← lag 4H          │
│  ▼ 測試階段                                                  │
│    功能測試  (陳大文 / 品保)            ▓▓▓▓▓▓              │
│    耐久測試  (陳大文 / 品保)            ▓▓▓▓▓▓▓▓▓▓▓         │
│                                              ↘               │
│  ▼ 結案階段                                                  │
│    結案報告  (王小明 / 研發)                  ▓▓▓▓▓▓        │
│                                                              │
│  ─────────────────────────────────────────────────────────  │
│  共 4 個步驟 · 關鍵路徑 52 小時 · 涉及 2 個群組              │
│  ⓘ 依目前參數略過 1 個步驟：安規審查（僅 A 線需要）           │
│                                                              │
│                       [ 上一步 ]  [ 建立 Case ]              │
└──────────────────────────────────────────────────────────────┘
```

底部摘要列出步驟數、關鍵路徑長度、涉及群組，讓使用者最後確認一次規模是否符合預期。

兩件在這個預覽裡才看得出來、事後才發現會很麻煩的事：

- **略過的步驟**——因參數條件而不生成的步驟明列於摘要，避免使用者以為模板漏了東西
- **等待時間（lag）**——依賴線上的 `⋯` 虛線段代表「前置做完還要等 N 小時才能接手」，這段時間不佔任何人力

按下「建立 Case」後直接導向該 case 的詳情頁，不回到總表——使用者剛建好，下一步幾乎一定是想看它。

---

## 4. 流程 B：Gantt 圖檢視

Case 詳情頁是系統的核心畫面。

```
┌─────────────────────────────────────────────────────────────────────────┐
│  ← Case 總表                                                            │
│  2026Q3 新產品導入 - 型號 A                          🔴 逾期             │
│  新產品導入流程 v3 · 目標 2026-08-15 18:00 · 建立者 王小明               │
│  ─────────────────────────────────────────────────────────────────────  │
│  進度 ████████████░░░░░░░░░░░░░  5/12 完成    預測完成 08-16 02:00 (+8H) │
│  ─────────────────────────────────────────────────────────────────────  │
│  [Gantt] [列表] [稽核紀錄]      分組: [無 ▾]  刻度: [小時 ▾]  [⚙ 顯示]  │
│                                                                          │
│  ┌── 任務欄 ──────────────┬── 時間軸 ────────────────────────────────┐ │
│  │                        │  08/13      08/14      08/15   ┊         │ │
│  │ 名稱      負責人  狀態 │  00  12  00  12  00  12  00  12 ┊         │ │
│  ├────────────────────────┼─────────────────────────────────┊─────────┤ │
│  │ my_task1  王小明  ✓    │  ▓▓▓▓▓                          ┊         │ │
│  │                        │  ░░░░                            ┊  ← 實際 │ │
│  │                        │      ↘                          ┊         │ │
│  │ my_task2  李小華  ▶    │      ▓▓▓▓▓                      ┊         │ │
│  │                        │       ░░░░░░░                    ┊         │ │
│  │                        │            ↘                    ┊         │ │
│  │ my_task3  陳大文  ○    │           ▒▒▒▒▒▒               ┊         │ │
│  │                        │              ▓▓▓▓▓▓             ┊         │ │
│  │                        │                          ↑今天  ┊🎯目標   │ │
│  └────────────────────────┴─────────────────────────────────┊─────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4.1 雙軌時間軸：Baseline vs Forecast

這是本系統相對於一般 Gantt 工具最關鍵的視覺差異。每個 task 佔用**兩條細 bar** 而非一條粗 bar：

| 軌道 | 內容 | 視覺 |
|---|---|---|
| **上軌 — Baseline** | 建立 case 時反推出的原始計畫，之後不再改變 | 實心、飽和度較低、細 |
| **下軌 — 實際 / 預測** | 已完成的用實際時間；未完成的用依目前進度重算的預測時間 | 已完成為實心、進行中為帶動畫的漸層、未來預測為斜線紋 |

兩條 bar 對齊比較，使用者一眼就能看出「這步比計畫晚了多少」。當兩者偏差超過門檻，該列右側出現偏差標籤（例如 `+8H`）。

**計畫外新增的任務只有一條 bar。** 事後插入的任務（流程 D）不在原始計畫裡，沒有 baseline 可比較，因此只畫預測軌，列名旁標「計畫外新增」，偏差欄顯示 `—`。硬畫一條不存在的計畫線只會誤導；留白反而讓「這個 case 事後多做了哪些計畫外的工作」看得見——那正是事後檢討時想知道的事。

#### 時間軸取景：以預測為準

**圖的時間範圍由預測決定，不含 baseline 與目標線。** baseline 是 ALAP（貼著目標日往回排），所以任何有餘裕的 case，它的 baseline 全都擠在目標日附近。若把 baseline 納入取景，一個「工作只有一週、目標在三週後」的 case 會被拉成四週寬，實際工作壓在左側五分之一，而 baseline 變成右邊一排孤零零、附近沒有任何 bar 的灰色殘跡——那不是參照，是雜訊。

因此：

- **落在畫面外的 baseline 影線不畫**，而不是裁切到邊緣。它的作用是當它所屬 bar 的參照，旁邊沒有 bar 的殘段沒有意義。
- **「現在」與「目標」落在畫面外時釘在邊緣**，並標出距離（例如 `target 19d 22h ▶`）。餘裕有多少必須看得見，只標方向不夠。
- 一旦有任務真的落後，它的預測會往後推越過 baseline，baseline 自然重新進入畫面——**需要對照的時候才會出現**。

**目標日期**以垂直的 🎯 標線呈現。**專案緩衝**是 🎯 之前一段獨立的斜紋區塊，標示「緩衝 8H」，已被消耗的部分改為紅色實心——使用者能直接看出「還剩多少可以燒」。

**預測完成時間**若超過目標線，超出的區段以紅色網底標出，並在頁首摘要顯示 `預測完成 08-16 02:00 (+8H)`。

**目標日期被改過時**，圖上同時保留原始目標線（淡色虛線）與現行目標線（實線 🎯），兩線之間標註差距，摘要列顯示「交期已調整 2 次」可展開查看歷程。交期被改動是事後檢討時最關鍵、也最容易被悄悄抹掉的資訊，必須留在畫面上。

改交期**不會動到 baseline**——那才是 baseline 的意義。若一個 case 已經偏離到原始計畫失去參考價值（例如專案重啟），case 設定裡有「重設 baseline」：以目前的預測覆寫計畫，原計畫完整存入歷史。這個操作刻意設計得有點麻煩（需權限、二次確認、明確告知「偏差數字會歸零」），因為重設 baseline 等於抹去「我們原本答應了什麼」，不該順手就能做。

### 4.2 依賴連線

DAG 依賴以貝茲曲線從前置 task 的 bar 尾端連到後續 task 的 bar 頭端。

- 一個 task 有多個前置時，多條線匯入同一點
- **含等待時間（lag）的依賴**在連線起點畫一段虛線並標註時長（`⋯ 4H`）。這段時間不佔用任何人力，視覺上必須和「有人在做事」的實心 bar 明確區隔——否則使用者會誤以為有人閒置了四小時
- Hover 某個 task 時，其**所有上游與下游**路徑高亮，其餘淡出至 30% 透明度
- 依賴線本身可點擊，觸發「在此處插入 task」（見流程 D）
- 依賴線密集時可用 `⚙ 顯示` 面板關閉，改以「前置」欄位文字呈現

### 4.3 關鍵路徑

`⚙ 顯示` 面板可開啟「標示關鍵路徑」。開啟後關鍵路徑上的 task bar 加上明顯外框與左側標記條。這條路徑上任一 task 延遲，整個 case 的完成時間就會延後——這是流程負責人最該盯的部分。

### 4.4 顏色語彙：顏色編碼「階段」，不是狀態

Gantt 上的**顏色代表階段（phase）**，同一階段共用一個色相，該階段的任務用同色的淺色版本。這讓整張圖從遠處就讀得懂「現在在哪個階段」，而不是只看得出一片藍綠紅。

每個階段最上方有一條**較粗的摘要 bar**，橫跨該階段最早開始到最晚結束，上方標示該階段的主要負責人。階段是人在規劃時的單位，值得有自己的一條 bar，而不只是一個標題——「測試階段過了沒」要能一眼回答，不必逐列讀。

狀態則由**其他三個管道**傳達，仍然不單靠顏色：

| 管道 | 內容 |
|---|---|
| 左側任務列的圖示 | `○ ◉ ▶ ✓ ✕ ⊘`（見下表） |
| bar 的填色處理 | `pending` 只有外框、`running` 加白邊、`done` 加深、`cancelled` 半透明 |
| tooltip 文字 | 狀態名稱與修飾（optional、計畫外新增） |

**只有 `failed` 會覆蓋階段顏色**，直接轉為紅色。失敗必須是刺眼的——黃色階段裡出現一條紅，正是要能立刻看見的東西。

### 4.5 狀態語彙

狀態由**圖示、填色處理、文字**三者傳達。顏色本身編碼的是階段（見 §4.4），所以狀態從來就不靠顏色單獨傳達：

| 狀態 | 圖示 | 顏色 | Bar 樣式 | 說明 |
|---|---|---|---|---|
| `pending` | ○ | 灰 | 空心細框 | 前置未完成，還不能開始 |
| `ready` | ◉ | 藍 | 實心 | 前置已完成，可以開始了 |
| `running` | ▶ | 藍 | 實心 + 流動動畫 | 進行中（API 執行中或人員處理中） |
| `done` | ✓ | 綠 | 實心 | 已完成 |
| `failed` | ✕ | 紅 | 實心 + 斜線紋 | API 執行失敗 |
| `cancelled` | ⊘ | 灰 | 半透明 + 刪除線 | 已取消 |
| *逾期修飾* | ⚠ | 橘框 | 疊加橘色外框 | 任何未完成狀態 + 已超過 baseline 結束時間 |
| *逾期未開始* | ⏰ | 橘框 | 左緣加粗橘色標記 | `ready` 但已超過 baseline 開始時間 |
| *可略過修飾* | ◇ | — | 虛線外框 | `optional` 任務，未完成也能結案 |
| *計畫外修飾* | ＋ | — | 單軌、無 baseline bar | 建立 case 之後才插入的任務 |

`ready` 狀態特別重要：它明確告訴執行者「輪到你了」。這也是「我的任務」清單的主要篩選條件。

**失敗但流程續行**（模板設定為失敗不擋主線的步驟）是一個容易誤解的狀態組合：該 task 顯示為紅色 ✕，但下游照常轉為 `ready`。此時 task 列右側加註「已略過失敗，流程繼續」的說明標籤，避免使用者看到紅色就以為卡住了。Case 健康度仍會記入這筆失敗。

**`optional` 不等於「不會拖累進度」。** 若有必要任務依賴某個 optional 任務，它延遲照樣推遲下游——虛線外框只表示「未完成也能結案」，不表示它在關鍵路徑之外。Tooltip 需明說這點。

### 4.6 縮放、刻度與分組

- **時間刻度**：小時 / 天 / 週 三段切換。系統依 case 總工期自動選擇初始刻度（< 3 天用小時，< 3 週用天，其餘用週）。
- **橫向縮放**：滾輪 + Ctrl 縮放，拖曳平移。`[今天]` 按鈕跳回目前時間。
- **分組**：預設依模板定義的**階段（phase）**分組並可摺疊；也可切換為依群組或負責人分組。收合時該組顯示一列摘要 bar（涵蓋組內最早開始到最晚結束）。跨部門流程用群組分組後可讀性提升很多。
- **篩選**：只看未完成 / 只看我的 / 只看某群組 / 只看關鍵路徑。

### 4.7 略過的步驟

因參數條件而未生成的步驟，在 Gantt 下方以一列唯讀摘要呈現：

```
ⓘ 本 case 略過 1 個步驟：安規審查（條件：產線類型 = A）        [ 展開 ]
```

這是刻意保留的資訊。使用者對照模板時若發現少了步驟，需要能立刻確認「是被條件略過」而不是「系統出錯」或「有人刪掉了」。略過的步驟無法在 case 內補回——若真的需要，用插入 task（流程 D）另外加。

### 4.8 列表視圖

同一份資料的表格呈現，欄位包含：名稱、負責人、群組、狀態、計畫起訖、實際起訖、偏差、前置任務。支援排序與 CSV 匯出。窄螢幕時自動採用此視圖。

---

## 5. 流程 C：編輯 Task

點擊任一 task（bar 或任務欄名稱）從右側滑出 drawer，Gantt 圖維持可見。

```
┌──────────────────────────────────────────────┐
│  my_task2                              ✕     │
│  來自 Task 模板 tt2 · ▶ 進行中               │
│  ────────────────────────────────────────    │
│                                              │
│  基本資訊                                    │
│    任務名稱   [ my_task2               ]     │
│    負責人     [ 李小華              ▾ ]     │
│    群組       [ 測試部              ▾ ]     │
│    工期       [ 14 ] [ 小時 ▾ ]  ← 已修改     │
│               目前 12H · 模板預設 10H         │
│    時間計算   ( ) 連續時間 24×7               │
│               (•) 工作日曆  [ 台灣標準 ▾ ]   │
│                                              │
│  依賴                                        │
│    前置任務   [my_task1 ✕]  [+ 新增]         │
│    後續任務   my_task3（唯讀）                │
│                                              │
│  任務參數                                    │
│    my_para1   [ 3                      ]     │
│    my_para2   [ test                   ]     │
│                                              │
│  時程                                        │
│    計畫  08-14 06:00 → 08-14 18:00           │
│    實際  08-14 08:30 → 進行中                 │
│    預測  08-14 08:30 → 08-14 20:30  (+2.5H)  │
│                                              │
│  ┌────────────────────────────────────────┐ │
│  │ ⚠️ 工期由 12H 改為 14H 將使 my_task3    │ │
│  │    延後 2H，預測完成 08-15 10:30         │ │
│  │    （仍在目標 08-15 18:00 之前）         │ │
│  └────────────────────────────────────────┘ │
│                                              │
│  執行紀錄                                    │
│    08-14 08:30  API my_function 已觸發        │
│    08-14 08:31  回報 running                  │
│                                              │
│         [ 取消 ]  [ 儲存變更 ]  [ ✓ 標記完成 ] │
└──────────────────────────────────────────────┘
```

### 設計要點

**即時影響預估。** 使用者一改工期或依賴，drawer 內立刻出現黃框說明「這會讓下游延後多少、會不會超過目標日期」，同時 Gantt 圖上受影響的 task 以虛線輪廓顯示新位置。**按下儲存前不會真的改變任何資料。** 這讓使用者敢於嘗試。

**工期欄位顯示模板預設值。** 讓使用者知道自己偏離了標準值多少。

**每個 task 各自選擇時間計算方式。** 自動化步驟（如「系統跑批次」）用連續時間 24×7；需要人力介入的步驟（如「主管審核」）用工作日曆，跳過週末與非工時。這個選擇直接影響反推結果，因此放在明顯位置並附說明。

**依賴編輯有防呆。** 新增前置任務的下拉選單會自動排除會造成循環的選項，而不是讓使用者選了才報錯。每條前置依賴可各自設定「等待時間」，用於「前置做完還要等 N 小時才能接手」的情況（養護、冷卻、等對方回覆）——這段時間在 Gantt 上是虛線，不佔任何人力。

**負責人顯示來源。** 若這個 task 的負責人是由角色推導而來，欄位下方標示「來自角色：品保負責人」。在此處改人只影響這一個 task；要一次改掉該角色的所有 task，改用 case 設定裡的「重新指派角色」。這個區別必須明講，否則使用者會以為改一個就全改了。

**失敗策略與可略過**在 drawer 底部的「進階」區塊，預設收合。它們多半在模板層就設定好，case 內調整是例外情況。

**權限不足時**，所有輸入欄位變為唯讀，底部按鈕替換為說明文字：「僅 李小華 或 測試部 成員可編輯此任務」。不隱藏欄位——使用者仍需要看得到內容。

---

## 6. 流程 D：在兩個 Task 之間插入新 Task

現實流程常常臨時多一步。這個操作要能在三次點擊內完成。

### 觸發方式

1. **點擊依賴連線** — hover 連線時中點出現 `⊕` 按鈕，點擊即插入
2. **Task 選單** — 任一 task 的 `⋯` 選單 →「在此之後插入」
3. **右鍵選單** — Gantt 空白區右鍵 →「新增任務」（不接任何依賴的獨立 task）

### 插入面板

```
┌──────────────────────────────────────────────┐
│  插入任務                              ✕     │
│  將插入於：my_task1 → my_task2               │
│  ────────────────────────────────────────    │
│                                              │
│  來源                                        │
│    (•) 使用 Task 模板                        │
│    ( ) 建立空白任務                          │
│                                              │
│  🔍 搜尋 Task 模板...                        │
│  ┌──────────────────────────────────────┐   │
│  │ ▸ bt1   資料備份       10H  🔌 API   │   │
│  │ ▸ tt4   主管審核        4H  ✋ 手動   │   │
│  │ ▸ tt5   環境檢查        2H  🔌 API   │   │
│  └──────────────────────────────────────┘   │
│                                              │
│  已選：tt4 主管審核                          │
│    任務名稱   [ 主管審核               ]     │
│    負責人     [ 張經理              ▾ ]     │
│    工期       [ 4 ] [ 小時 ▾ ]               │
│                                              │
│  依賴接法                                    │
│    (•) 串接：my_task1 → 新任務 → my_task2    │
│    ( ) 並行：my_task1 → 新任務（不接下游）   │
│                                              │
│  ┌────────────────────────────────────────┐ │
│  │ ⚠️ 插入後關鍵路徑增加 4 小時            │ │
│  │    預測完成 08-16 06:00（超過目標 12H）  │ │
│  └────────────────────────────────────────┘ │
│                                              │
│                    [ 取消 ]  [ 插入 ]        │
└──────────────────────────────────────────────┘
```

### 設計要點

- Task 模板清單標示 🔌 API 自動完成 / ✋ 手動完成，讓使用者知道插進去之後誰負責收尾
- 選定模板後自動帶入預設工期與參數，但都可即時覆寫
- **依賴接法**明確給兩個選項：串接（插在中間，會延長路徑）或並行（掛在旁邊，不影響下游）。這是使用者最容易搞混的地方，用文字寫清楚而不是靠圖示暗示。
- 插入前顯示對關鍵路徑與目標日期的影響，同樣在 Gantt 上以虛線預覽新位置
- 插入是**只影響本 case** 的操作，不會回寫 template。面板底部有一行小字說明這點，並提供「同時更新來源模板」的連結（僅 Template Admin 可見）

### 刪除 Task

從 `⋯` 選單刪除時，若該 task 位於依賴鏈中間，詢問下游要如何處理：

- **接回上游**（預設）：`A → B → C` 刪除 B 後變成 `A → C`
- **斷開**：C 失去前置，變成可立即開始

已經 `done` 或 `running` 的 task 不可刪除，只能標記為 `cancelled`，保留稽核痕跡。

---

## 7. 流程 E：標記 Task 完成

### 7.1 手動完成

點擊 drawer 底部或 task 選單的「✓ 標記完成」：

```
┌──────────────────────────────────────────────┐
│  標記完成：my_task2                          │
│  ────────────────────────────────────────    │
│  實際完成時間                                │
│    (•) 現在  2026-08-14 20:30                │
│    ( ) 指定  [            ] [        ]       │
│                                              │
│  備註（選填）                                │
│  ┌──────────────────────────────────────┐   │
│  │ 第 3 批因設備調校延後 2 小時          │   │
│  └──────────────────────────────────────┘   │
│                                              │
│  ⓘ 完成後 my_task3 將轉為「可開始」，          │
│    並通知 陳大文。                            │
│                                              │
│                  [ 取消 ]  [ 確認完成 ]      │
└──────────────────────────────────────────────┘
```

- 允許**回填過去時間**（實務上常常是做完了才想到要回報），但不允許填未來時間
- 完成後明確告知「接下來會發生什麼」——誰會被通知、哪個 task 變成可開始
- 若該 task 綁定了 `task_api` 且正在執行中，會額外提示：「此任務的 API 仍在執行中，手動標記完成將中止等待。確定要繼續嗎？」

### 7.2 自動完成（task_api）

綁定 `task_api` 的 task 在前置條件滿足後，由背景 worker 自動觸發後端註冊的處理函式。使用者在 UI 上看到的是：

| 階段 | Bar 呈現 | 任務欄呈現 |
|---|---|---|
| 等待觸發 | `ready` 藍色實心 | ◉ 可開始 |
| 已觸發、執行中 | 流動動畫 | ▶ 執行中 · `my_function` · 已 12 分鐘 |
| 輪詢中（長任務） | 流動動畫 | ▶ 執行中 · 上次檢查 30 秒前 |
| 成功 | 綠色實心 | ✓ 已完成 · 由 API 自動完成 |
| 失敗 | 紅色 + 斜線紋 | ✕ 失敗 · 第 2 次重試中 (3/3) |
| 重試耗盡 | 紅色 + 斜線紋 | ✕ 失敗 · [ 重新執行 ] [ 手動完成 ] |

失敗時：

- 該 task bar 上出現紅色驚嘆號徽章，Case 總表該列同步標紅
- 通知該 task 的 owner 與 case 建立者
- Drawer 的「執行紀錄」區塊完整顯示每次嘗試的時間、耗時、錯誤訊息（可展開查看完整堆疊）
- 提供兩條出路：**重新執行**（再跑一次 API）或 **手動完成**（人工介入處理完後手動結案）

**手動完成永遠是可用的逃生門。** 自動化失敗不該讓整個流程卡死。

### 7.3 完成後的連鎖反應

任一 task 完成時，畫面即時（WebSocket 推送）反映：

1. 該 task bar 轉為綠色
2. 下游 task 若前置全數完成，狀態由 `pending` → `ready`，bar 由空心轉實心
3. 預測時間軌重算，若預測完成時間變動，頁首摘要的數字與顏色更新
4. 若整個 case 全數完成，頁首狀態變為 🟢 已完成，並顯示實際總工期與相對目標的偏差

---

## 8. 流程 F：Case 總表

系統首頁，回答「所有流程現在如何」。

```
┌───────────────────────────────────────────────────────────────────────────┐
│  Case 總表                                              [ + 建立 Case ]   │
│  ───────────────────────────────────────────────────────────────────────  │
│  🟢 準時 12    🟡 有風險 3    🔴 逾期 2    ✓ 已完成 45    ⊘ 已取消 1      │
│  ───────────────────────────────────────────────────────────────────────  │
│  🔍 搜尋   [模板 ▾] [狀態 ▾] [負責人 ▾] [群組 ▾] [目標日期 ▾]   [⬇ CSV] │
│  ───────────────────────────────────────────────────────────────────────  │
│  狀態 │ Case 名稱              │ 模板      │ 進度      │ 目標    │ 預測   │
│  ─────┼────────────────────────┼───────────┼───────────┼─────────┼────────│
│  🔴   │ 2026Q3 導入 - 型號 A   │ 導入 v3   │ ████░░ 5/12│ 08-15   │ +8H ⚠ │
│       │ 卡在 my_task2 · 李小華  │           │           │ 18:00   │        │
│  ─────┼────────────────────────┼───────────┼───────────┼─────────┼────────│
│  🟡   │ 8 月關帳                │ 關帳 v7   │ ██████ 6/8 │ 08-05   │ 準時   │
│       │ 進行中 對帳 · 財務部     │           │           │ 12:00   │        │
│  ─────┼────────────────────────┼───────────┼───────────┼─────────┼────────│
│  🟢   │ 2026Q3 導入 - 型號 B   │ 導入 v3   │ ██░░░░ 2/12│ 09-01   │ 準時   │
│       │ 進行中 需求確認 · 王小明 │           │           │ 18:00   │        │
│  ───────────────────────────────────────────────────────────────────────  │
│                                                    ‹ 1 2 3 ›  每頁 [25 ▾] │
└───────────────────────────────────────────────────────────────────────────┘
```

### 8.1 健康度定義

健康度回答的是「**現在該不該緊張**」，而不只是「預測有沒有超過交期」。模板若設定了專案緩衝，判定改為比較兩個比例——**已消耗的緩衝**與**已完成的工作量**：

| 標記 | 條件（有設定緩衝時） |
|---|---|
| 🟢 準時 | 緩衝消耗比例 ≤ 進度比例 |
| 🟡 有風險 | 緩衝消耗比例 > 進度比例，但緩衝尚未用罄；或有 task 逾期未開始 / 執行失敗 |
| 🔴 逾期 | 緩衝已用罄（預測完成時間超過目標日期） |
| ✓ 已完成 | 所有必要 task 已結算 |
| ⊘ 已取消 | Case 被手動取消 |

**為什麼要這樣比。** 做完三成工作卻燒掉七成緩衝，即使預測完成時間還在交期之內，也該亮黃燈——因為照這個速度下去一定會爆。單純比較「預測完成 vs 目標日期」看不出這件事，等到它看得出來時通常已經來不及了。

模板未設定緩衝（預設）時退回簡單判定：預測完成時間超過目標日期即為 🔴，否則 🟢；有 task 逾期未開始或執行失敗則為 🟡。

**進度以工期加權計算**，而非任務數比例。`5/12` 會把 10 分鐘的通知和 12 小時的測試算成等值，嚴重失真。進度條顯示加權百分比，旁邊仍附任務數（`62% · 5/12`）——百分比反映真實工作量，任務數讓使用者知道還有幾件事要處理。

頂部的統計數字可點擊，等同於套用該狀態篩選。

### 8.2 表格設計

- **每列兩行**：主行是 case 識別資訊，副行是「現在卡在哪一步、在誰身上」——這是流程負責人最常問的問題，不該需要點進去才看得到
- **進度欄**同時有進度條與 `5/12` 數字
- **預測欄**只顯示相對偏差（`+8H` / `準時`）而非絕對時間，掃視效率較高；hover 顯示絕對時間
- 點列任一處進入 case 詳情；`⋯` 選單提供複製、取消、刪除
- 預設排序：先依健康度（紅 → 黃 → 綠），同健康度內依目標日期升冪。**最需要注意的排在最上面。**

### 8.3 篩選與批次

篩選條件寫入 URL query，可直接分享連結給同事。常用組合可存成「我的視圖」（例如「我群組的逾期案件」）釘選在篩選列上。

**搜尋範圍**涵蓋三處：case 名稱（權重最高）、**任務名稱**、以及字串型參數值。搜尋任務名稱是刻意的——「哪些 case 做過安規審查」是實際會被問到的問題；搜尋參數值則讓使用者能用料號或批號直接找到相關 case。搜尋不涵蓋備註與稽核紀錄，那些內容雜訊高、會稀釋結果品質。

多選核取方塊支援批次取消、批次改負責人、批次匯出。

**封存**：完成或取消滿 90 天的 case 自動封存，不再出現在預設清單與頂部統計中。封存不是刪除——篩選器可切換「包含已封存」，資料永久保留（它也是模板健檢報表的來源）。

---

## 9. 流程 G：Template 編輯器

模板管理員的主要工作區。同一份模板提供**三種可切換且雙向同步**的編輯模式。

```
┌───────────────────────────────────────────────────────────────────────────┐
│  ← Template 庫    新產品導入流程                v4 草稿 · 未儲存變更      │
│  ───────────────────────────────────────────────────────────────────────  │
│  [ 表單 ] [ 流程圖 ] [ YAML ]              [試算預覽] [儲存草稿] [發布 v4]│
│  ───────────────────────────────────────────────────────────────────────  │
│                                                                            │
│  ┌── 流程圖模式 ─────────────────────────────────────────────────────┐   │
│  │                                                                    │   │
│  │    ┌──────────┐      ┌──────────┐      ┌──────────┐             │   │
│  │    │ my_task1 │─────▶│ my_task2 │─────▶│ my_task3 │             │   │
│  │    │ tt1  12H │      │ tt2  12H │      │ tt3  12H │             │   │
│  │    │ 研發部   │      │ 測試部   │      │ 品保部   │             │   │
│  │    └──────────┘      └──────────┘      └──────────┘             │   │
│  │                            │                                      │   │
│  │                            └──────▶┌──────────┐                  │   │
│  │                                    │ my_task4 │                  │   │
│  │                                    │ tt5   4H │                  │   │
│  │                                    └──────────┘                  │   │
│  │                                                                    │   │
│  │                      [ + 從 Task 庫拖入 ]                          │   │
│  └────────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│  ┌── 驗證 ────────────────────────────────────────────────────────────┐  │
│  │ ✕ my_task5 的 requirement 參照了不存在的任務 "my_taskX"            │  │
│  │ ⚠ 參數 my_para2 定義後未被任何任務使用                              │  │
│  │ ⚠ my_task4 沒有後續任務，將直接對齊目標日期                         │  │
│  └────────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────────────────┘
```

### 9.1 三種編輯模式

| 模式 | 適合對象 | 特色 |
|---|---|---|
| **表單** | 不熟悉 YAML 的一般管理員 | 參數區與流程區各為可摺疊的表單卡片，逐欄填寫，附完整說明與型別選單 |
| **流程圖** | 需要看清結構的人 | 節點拖曳、連線建立依賴、拖入 Task 模板。**DAG 分支在此模式下最直觀** |
| **YAML** | 熟手 | Monaco 編輯器，含語法高亮、schema 自動補完、即時錯誤標示。支援直接貼上整份 YAML |

三個模式共用同一份記憶體中的模板物件，切換不遺失變更。YAML 模式若有語法錯誤，切換到其他模式時會提示先修正。

流程圖模式中，三種特殊節點有各自的視覺標記，讓管理員一眼看出流程的動態部分：

| 節點類型 | 標記 |
|---|---|
| 條件式任務 | 節點左上角 `?` 徽章 + 虛線外框，hover 顯示條件式 |
| 可略過任務 | 節點虛線外框（同 Gantt 的 ◇ 修飾） |

模板若使用舊欄位名（`task_name` / `task_template` / `target_duration` 等），YAML 模式會以底線提示並提供「一鍵正名」；舊名永遠可正常運作，正名純屬可讀性改善。

### 9.2 即時驗證

編輯過程持續驗證，錯誤與警告分列於底部面板，點擊可跳至問題位置：

**錯誤（阻擋發布）**

- 依賴循環（`A → B → C → A`），流程圖上以紅色高亮該環路
- `requirement` 參照不存在的任務名稱
- 任務名稱重複，或展開後的任務名稱撞名
- 工期格式不合法
- 參數引用 `{{ para.xxx }}` 中的 `xxx` 未定義
- 引用未宣告的角色
- `owner: { same_as: ... }` 參照不存在的任務，或形成循環
- 引用不存在的 Task 模板
- 等待時間（lag）為負值
- 必填欄位缺漏

**警告（可發布）**

- 定義了但沒用到的參數或角色
- 沒有後續任務的孤立終點（多個終點都會各自對齊目標日期，這通常是預期的，但值得提醒）
- 沒有前置任務的孤立起點超過一個
- 工期為 0
- 任務沒有指定負責人，也沒有任何後備來源
- 條件式任務同時是流程終點（條件為 false 時會改變流程的收尾結構）
- 條件恆為 true 或恆為 false（多半是筆誤）

### 9.3 試算預覽

`[試算預覽]` 開啟浮層，讓管理員填入假參數與假目標日期，立刻看到這份模板會生成什麼樣的 Gantt——**不會真的建立 case**。

**模板一旦使用條件式任務，這就從便利功能變成必要步驟。** 這類模板的最終形狀取決於參數，編輯器在靜態檢查階段只能驗證語法與參照，無法確認「條件都為 false 時流程會不會斷開」。因此模板含條件式任務時，發布前會提示「此模板的流程結構隨參數變動，建議至少試算一組參數」，並在浮層內提供一鍵套用「全部預設值」「所有條件為真」「所有條件為假」三組情境。

浮層明確列出三件事：完整的 Gantt、**因條件被略過的步驟清單**、以及關鍵路徑長度。可快速切換不同參數組合比較結果，例如「測試工時 = 12 時總長 36 小時，= 24 時總長 48 小時」。

### 9.4 模板健檢報表

模板編輯器的 `[健檢]` 分頁回答一個管理員原本只能靠感覺的問題：**我們寫的工期估得準嗎？**

```
┌───────────────────────────────────────────────────────────────┐
│  新產品導入流程 · 健檢            區間 [ 最近 6 個月 ▾ ]      │
│  ───────────────────────────────────────────────────────────  │
│  47 個 case · 準時率 72% · 平均緩衝消耗 61%                    │
│  ───────────────────────────────────────────────────────────  │
│  步驟          計畫    實際中位   P80    超時率   關鍵路徑率   │
│  ─────────────────────────────────────────────────────────── │
│  需求確認      12H     10H       14H     31%      12%         │
│  測試驗證      12H     16H ⚠     22H     68%      91% 🔥      │
│  結案報告      12H     11H       13H     24%      45%         │
│  ───────────────────────────────────────────────────────────  │
│  🔥 瓶頸：測試驗證                                             │
│     91% 的 case 中位於關鍵路徑，且 68% 超出計畫工期            │
│     建議：預設工期由 12H 調整為 16H（中位數）或 22H（P80）     │
│                                    [ 套用中位數 ] [ 套用 P80 ] │
└───────────────────────────────────────────────────────────────┘
```

這份報表不需要任何額外的資料收集——系統從第一天起就逐筆記錄了每個任務的計畫與實際時間，只是原本沒人去查。它讓模板從「一次寫好就不再管」變成可以持續校準的東西，也是唯一能回答「我們的流程到底哪一步在拖」的地方。

顯著低估的步驟（中位數超出計畫 20% 以上、樣本數 ≥ 10）會在編輯器的工期欄位旁主動顯示提示與一鍵套用建議值，不必等管理員自己想到要來看報表。

### 9.5 週期性自動建立

「月結關帳」這類流程本來就是每月固定跑的，不該要人記得手動建。模板設定頁可開啟排程：

```
週期建立    [✓] 啟用
  頻率      [ 每月 ▾ ]  [ 5 ] 號  [ 09:00 ]
  目標日期  建立後 [ 3 ] 天
  Case 名稱 [ {{年}}年{{月}}月關帳          ]
  角色指派  沿用上次 · 專案經理 = 林經理     [ 變更 ]
  ─────────────────────────────────────────
  下次建立  2026-08-05 09:00
  上次建立  2026-07-05 09:00 → 7月關帳（已完成）
                                  [ 立即建立一次 ]
```

三個刻意的行為，都寫在設定頁的說明文字裡：

- 排程使用模板的**最新已發布版本**，不釘選版本——週期性流程通常希望跟著最新定義走
- 建立失敗（例如必填角色沒指派）**不重試**，改為通知設定者並停用排程。每分鐘失敗一次洗版比沒建到更糟
- 錯過的執行**不補建**。系統停機三天後補建三個一建立就逾期的 case 沒有意義，改為通知「已錯過 N 次排程」

### 9.6 新增、匯出與匯入

模板庫頁面的 `[+ 新增模板]` 進入貼上 YAML 的畫面，這是**從零建立一份模板的唯一入口**。原本沒有這個入口：列表只連到已存在的模板，而編輯器載入時要求名稱已存在，於是第一步只能在命令列完成。

新增畫面收的是 YAML 而非編輯器原始碼模式用的 JSON——參考文件與所有範例都是 YAML 寫的，而後端本來就會解析。畫面預帶一份可直接送出的骨架，避免面對空白輸入框。

**建立後不自動跳轉到編輯器。** 匯入可能悄悄略過一個同名但內容不同的 task 模板（保留既有的），也可能引用了還沒設定的憑證，這兩件事都需要被看見；直接跳走會把它們捲過去。結果頁列出這些，再由使用者決定要不要進編輯器。

模板本來就是 YAML，`[匯出]` 產生單一自我完備的檔案（含所引用的 task 模板與行事曆）。用途是進 git 版控、在測試與正式環境之間搬移、以及災難復原。

匯入一律進入**草稿**，絕不直接覆寫已發布版本，且匯入前先跑完整驗證——驗證失敗則完全不寫入，不會留下半套模板。名稱已存在時顯示差異並讓使用者選擇「建立新草稿」或「另存新模板」。

**憑證不隨模板走。** API 憑證只匯出名稱不匯出內容，目標環境需自行設定同名憑證。匯入時找不到對應憑證會列為警告而非阻擋——先讓流程搬過去，設定之後再補。

### 9.7 版本管理

- 模板有 **草稿 / 已發布** 兩種狀態。編輯中的變更存為草稿，不影響任何人。
- `[發布]` 產生新版本號。已發布版本**不可再修改**，只能基於它建立新草稿。
- 版本歷史頁顯示每個版本的發布時間、發布者、變更摘要，並提供任兩版之間的 **diff**（YAML 並排差異 + 流程圖結構差異標色）。
- **既有 case 完全不受影響。** Case 在建立時已完整快照模板內容。版本頁明確標示「v3 有 8 個進行中的 case，它們將繼續使用 v3 的定義」，避免管理員誤以為改了模板就會影響進行中的流程。

### 9.8 Task 模板編輯器

結構較簡單的獨立畫面：

```
┌──────────────────────────────────────────────┐
│  Task 模板：bt1                               │
│  ────────────────────────────────────────    │
│  識別碼        [ bt1                    ]     │
│  顯示名稱      [ 資料備份               ]     │
│  預設工期      [ 10 ] [ 小時 ▾ ]              │
│  時間計算      (•) 連續 24×7  ( ) 工作日曆    │
│  預設負責人    [ 資訊部的 lead       ▾ ]      │
│                （流程未指定時的後備）          │
│  逾期預警      [ 2 ] 小時前通知               │
│                                              │
│  參數                                        │
│    ┌────────────────────────────────────┐   │
│    │ my_para1  [str ▾]  預設 [        ] │✕│  │
│    │ my_para2  [int ▾]  預設 [   1    ] │✕│  │
│    └────────────────────────────────────┘   │
│                            [ + 新增參數 ]     │
│                                              │
│  完成方式                                    │
│    ( ) 僅手動                                │
│    (•) 呼叫 API                              │
│        函式  [ 🔧 呼叫 HTTP API      ▾ ]     │
│              ├ 🔧 呼叫 HTTP API（內建）      │
│              ├ 🔧 等待外部回報（內建）        │
│              └ my_function ✓ 已註冊          │
│        ─────────────────────────────         │
│        網址  [ https://ci.internal/… ]       │
│        方法  [ POST ▾ ]                      │
│        憑證  [ ci_token            ▾ ]       │
│        成功條件 [ $.status == 'SUCCESS' ]    │
│        ─────────────────────────────         │
│        模式  [ 觸發後輪詢            ▾ ]     │
│        逾時  [ 30 ] 分鐘                     │
│        重試  [ 3 ] 次，間隔 [ 5 ] 分鐘       │
│        輪詢間隔 [ 60 ] 秒                    │
│        ✓ 允許手動覆寫完成                     │
│                                              │
│  失敗時                                      │
│    (•) 擋住流程，等待人工處理                 │
│    ( ) 記錄失敗但繼續往下走                   │
│    ( ) 取消整個 Case                          │
│                                              │
│  ⓘ 此模板被 3 個 Gantt 模板使用               │
│                            [ 儲存 ]          │
└──────────────────────────────────────────────┘
```

**下拉選單分成兩區：內建 handler 與自訂函式。**

選單最上方是 🔧 **內建 handler**——「呼叫 HTTP API」「等待外部回報」等，選了之後直接在同一頁填網址、方法、憑證與成功條件即可完成設定。這讓「接一個新系統」從「請工程師寫程式、review、部署」變成「模板管理員填個表單」，是自動化功能會不會真的被用起來的關鍵。真正需要複雜邏輯時才寫自訂 handler。

下方是**自訂函式**區，只列出後端實際已註冊的名稱，並顯示註冊狀態與最後執行時間。這避免了打錯字要等到 case 跑起來才發現的問題。若某個已使用的函式在後端被移除，此處會標紅警示。

**憑證用選的，不用貼。** 密鑰存在獨立的憑證庫，模板裡只留名稱——否則模板一匯出就把密鑰一起送出去了。

底部提示此模板被幾個 Gantt 模板使用，修改前知道影響範圍。

---

## 10. 我的任務

跨 case 的個人待辦清單，是執行者的入口。

```
┌───────────────────────────────────────────────────────────────┐
│  我的任務                                                      │
│  ─────────────────────────────────────────────────────────────│
│  [ 待我處理 (3) ] [ 進行中 (1) ] [ 已完成 ]     [群組任務 ▾]  │
│  ─────────────────────────────────────────────────────────────│
│  ⚠ 逾期                                                       │
│    my_task2 · 2026Q3 導入 - 型號 A                            │
│    應於 08-14 18:00 完成 · 已逾期 2 小時      [ ✓ 完成 ]      │
│  ─────────────────────────────────────────────────────────────│
│  ◉ 可開始                                                     │
│    對帳 · 8 月關帳                                            │
│    08-04 09:00 → 08-04 17:00 · 剩 6 小時      [ ✓ 完成 ]      │
│  ─────────────────────────────────────────────────────────────│
│  ○ 即將輪到                                                   │
│    需求確認 · 2026Q3 導入 - 型號 B                            │
│    等待 前置作業 完成（預計 08-20 10:00）                      │
└───────────────────────────────────────────────────────────────┘
```

- 依緊急度排序：逾期 → 可開始 → 即將輪到
- 可直接在清單上「✓ 完成」，不必進入 case
- `群組任務` 切換顯示同群組其他成員的任務（因為權限模型允許同組代打）

---

## 11. 通知

| 事件 | 通知對象 | 管道 |
|---|---|---|
| Task 轉為 `ready` | task owner | 站內 + Email |
| Task 即將逾期（baseline 結束前 2 小時） | task owner | 站內 |
| **Task 逾期未開始**（已過計畫開始時間仍未動工） | task owner + 群組 lead | 站內 + Email |
| Task 已逾期 | task owner + case 建立者 | 站內 + Email |
| `task_api` 執行失敗（重試耗盡） | task owner + case 建立者 | 站內 + Email |
| Case 預測完成時間首次超過目標 | case 建立者 | 站內 + Email |
| Case 完成 | case 建立者 | 站內 |
| 被指派為 task owner | 新 owner | 站內 |
| Task 轉 `ready` 但無負責人 | 該 task 所屬群組的 lead | 站內 + Email |
| Case 完成時自動取消未做的 optional task | 該 task owner | 站內 |

**「逾期未開始」是這張表裡唯一真正可行動的告警。** 等到一個任務的結束時間逾期，事情早就無法挽回；但「該開始卻還沒開始」時還有機會催人、換人或縮減範圍。這也是為什麼它同時進入 Case 總表的健康度判定與「我的任務」的排序權重。

站內通知在頂部鈴鐺呈現，未讀有紅點。每則通知可直接跳至對應 task。

**管道可選**：站內通知一律開啟，外送管道（Email，未來可加 Slack / Teams / LINE）由使用者在個人設定中依通知類型個別選擇。有些人希望所有事都進 Email，有些人只要逾期才吵他。

**避免通知疲勞**：同一 case 的多則同類通知在 5 分鐘內合併為一則摘要；同一任務的同類告警只發一次，不會每次掃描都重發。

---

## 12. 設計語彙

### 12.1 顏色

顏色僅作為輔助，所有狀態都同時有圖示或紋理標示。配色需通過 WCAG AA 對比度，並在色盲模擬下仍可區分。

| 語義 | 用途 |
|---|---|
| 中性灰 | `pending`、`cancelled`、次要文字、格線 |
| 主色藍 | `ready`、`running`、互動元件、連結 |
| 成功綠 | `done`、準時 |
| 警示黃 | 有風險、警告、即時影響提示框 |
| 危險紅 | `failed`、逾期、驗證錯誤 |
| Baseline | 主色藍的低飽和版本，與 forecast 明確區隔 |

### 12.2 空狀態

| 畫面 | 空狀態內容 |
|---|---|
| Case 總表（無 case） | 插圖 + 「還沒有任何 Case」+ `[ 從模板建立第一個 Case ]` |
| Case 總表（篩選無結果） | 「沒有符合條件的 Case」+ `[ 清除篩選 ]` |
| Template 庫（無模板） | 「還沒有流程模板」+ `[ 建立模板 ]` + 連結到模板撰寫說明 |
| 我的任務（無待辦） | ✓ 圖示 + 「目前沒有待處理的任務」 |

### 12.3 載入與錯誤

- **骨架屏**用於 Gantt 圖與表格初次載入，維持版面不跳動
- **樂觀更新**用於標記完成等操作：UI 立即反映，失敗則回滾並顯示 toast
- **錯誤 toast** 說明「發生了什麼 + 可以怎麼做」，例如「無法儲存：此任務已被李小華修改，請重新整理後再試」
- **連線中斷**時頂部出現持續橫幅「即時更新已中斷，正在重新連線…」，恢復後自動重整資料

---

## 13. 可及性與響應式

### 可及性

- 狀態**不單靠顏色**傳達，一律搭配圖示與文字（見 4.4）
- 所有互動元件可鍵盤操作；Gantt 圖支援方向鍵在 task 間移動、Enter 開啟 drawer、Space 標記完成
- Gantt 圖提供對等的「列表視圖」作為螢幕閱讀器的可存取替代，並以 `aria-live` 播報狀態變更
- 焦點指示清晰可見，模態框有焦點鎖定
- 支援作業系統的深色模式與減少動態效果偏好（`prefers-reduced-motion` 時關閉流動動畫）

### 響應式

| 寬度 | 行為 |
|---|---|
| ≥ 1280px | 完整體驗：Gantt 圖 + 側邊 drawer 並存 |
| 768–1279px | drawer 改為覆蓋式；任務欄可收合 |
| < 768px | Gantt 圖自動切換為列表視圖；主要支援「查看進度、標記完成」；不支援模板編輯（顯示提示請改用桌面） |

模板編輯與 Gantt 結構調整是桌面情境，行動端只保證「看進度、回報完成」這條路徑順暢。

---

## 14. 對照使用者原始需求

| 原始需求 | 對應章節 |
|---|---|
| 1. 從 template 選擇、輸入參數與目標日期、生成 gantt | [流程 A](#3-流程-a從-template-建立-case) |
| 2. 每個 task 可編輯、可在兩 task 間插入新 task、task 也有模板 | [流程 C](#5-流程-c編輯-task)、[流程 D](#6-流程-d在兩個-task-之間插入新-task)、[9.5](#95-task-模板編輯器) |
| 3. 手動標記結束或經 task_api 自動標記 | [流程 E](#7-流程-e標記-task-完成) |
| 4. Gantt case 總表 | [流程 F](#8-流程-fcase-總表) |
| 5. 網頁圖形化表現 | [流程 B](#4-流程-bgantt-圖檢視) |
| 6. Template 編輯器 | [流程 G](#9-流程-gtemplate-編輯器) |
